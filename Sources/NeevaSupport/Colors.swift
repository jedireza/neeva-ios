import SwiftUI

extension Color {
    static let searchHistoryEntry = Color("SearchHistoryEntry", bundle: .module)
    static let spaceIconBackground = Color("SpaceIconBackground", bundle: .module)
}
